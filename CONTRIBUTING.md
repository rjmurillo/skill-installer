# Contributing

## CodeQL setup

See `.codeql/README.md` for prerequisites, configuration details, and the helper script to run the GitHub-equivalent scan locally (artifacts are stored under `.codeql/`).
